import App from "./App";

const routes = [
  {
    name: "Lobby",
    routes: [
      {
        path: "/",
        text: "Lobby",
        component: App,
      },
    ],
  },
];

export default routes;
